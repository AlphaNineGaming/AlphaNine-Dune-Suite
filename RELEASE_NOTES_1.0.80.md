# AlphaNine Dune Suite 1.0.80

## Compact dashboard controls

- Adds a `Hide Banner` control to the main dashboard banner.
- Collapses the banner into a slim `Show Banner` row so it remains easy to restore.
- Remembers the banner preference across app launches.
- Moves About, Discord, and Buy Me a Coffee beside UI Mode in the top bar.
- Removes the duplicate sidebar actions while keeping the legal notice in place.
- Keeps the utility controls visible and free of horizontal overflow on narrower windows.

## Verification

- Passed the rendered Suite UI JavaScript syntax checks.
- Passed application icon packaging and runtime wiring checks.
- Verified the packaged app contains the banner state logic and relocated top-bar controls.
- Verified the Windows executable requests administrator privileges.
